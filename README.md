# 🃏 Project QQ: Poker Account Manager & Dashboard

Этот скрипт (`QQ Helper`) предназначен для автоматизации мониторинга покерных аккаунтов в Google Sheets. Он агрегирует данные из логов и списка аккаунтов, рассчитывает время отдыха, определяет активные сессии и генерирует удобный дашборд для оператора.

## 🚀 Основные возможности

  * **Мониторинг статусов:** Автоматически определяет, находится ли аккаунт в игре, на паузе или готов к запуску (Rested).
  * **Расчет времени:** Вычисляет продолжительность текущей сессии или время простоя (отдыха) с момента последней игры.
  * **Гибкий парсинг заголовков:** Распознает столбцы по синонимам (например, `start time` и `время начала сессии`), что позволяет менять названия колонок без поломки скрипта.
  * **Генерация отчетов:** Создает 4 сводные таблицы на листе `QQ_Helper`, ранжированные по приоритету.

-----

## ⚙️ Установка и Настройка

### 1\. Требования к Таблице

Скрипт ожидает наличие следующих листов (имена настраиваются в `CONFIG`):

| Имя листа | Описание |
| :--- | :--- |
| **`Accs`** | Реестр всех аккаунтов, комплектов и их статических статусов. |
| **`QQpoker`** | Лог игровых сессий (история рук, время начала/конца). |
| **`QQ_Helper`** | Лист для вывода отчета (создается/очищается автоматически). |
| **`QQ_CORE`** | Системный лист (создается автоматически, если нет). |

### 2\. Структура Данных (Заголовки)

Скрипт ищет столбцы по ключевым словам. Убедитесь, что ваши таблицы содержат столбцы с названиями из списка ниже (регистр не важен):

#### Лист `Accs` (Аккаунты)

  * **Nickname:** `никнейм`, `ник`, `account`
  * **Set/PC:** `комплект`, `pc`
  * **Status:** `статус`, `status`
  * **Brain ID:** `brain id`, `bid` *(Необязательно для комплектов S59)*

#### Лист `QQpoker` (Логи)

  * **Nickname:** `никнейм`, `ник`, `account`
  * **Date:** `date`, `дата`
  * **Start Time:** `start time`, `время начала сессии`
  * **End Time:** `end time`, `время окончания`
  * **Limits:** `limit`, `лимит`

### 3\. Установка скрипта

1.  Откройте вашу Google Таблицу.
2.  Перейдите в `Расширения` -\> `Apps Script`.
3.  Скопируйте код скрипта в редактор.
4.  Нажмите **Сохранить** и перезагрузите таблицу.

-----

## 🖥️ Использование

После перезагрузки таблицы в верхнем меню появится пункт **QQ Helper**.

1.  Нажмите **QQ Helper** -\> **Обновить отчёты**.
2.  При первом запуске Google запросит разрешение на доступ к данным — предоставьте его.
3.  Скрипт проанализирует данные и обновит лист `QQ_Helper`.

### Логика Статусов

Скрипт присваивает каждому аккаунту **вычисляемый статус** (`computedStatus`):

  * **🟢 In-Play (В игре):** В логах есть запись о начале сессии без указания времени окончания (`endTime` пустое).
  * **🔵 Rested (Отдохнул):** С момента окончания последней сессии прошло более **7 часов** (настраивается).
  * **🟠 Paused (На паузе):** С момента окончания последней сессии прошло менее 7 часов.

-----

## 📊 Структура Дашборда (Лист QQ\_Helper)

Скрипт генерирует 4 таблицы:

1.  **Основная таблица:** Полный список всех активных аккаунтов со статусами и временем.
2.  **Готовы к запуску:** Аккаунты, у которых статус в реестре `Ready` **И** они отдохнули (`Rested`).
3.  **В игре:** Аккаунты, которые прямо сейчас находятся в сессии (показано время в игре).
4.  **Обратить внимание:** Аккаунты со статусом `SLA` (проблемные/требующие проверки).

-----

## 🔧 Конфигурация (CONFIG)

В начале кода находится объект `CONFIG`, где вы можете изменить параметры без переписывания логики:

```javascript
const CONFIG = {
  sheet_log: 'QQpoker',       // Имя листа с логами
  sheet_accs: 'Accs',         // Имя листа с аккаунтами
  
  accs_row_limit: 75,         // Сколько строк аккаунтов считывать
  rest_threshold_hours: 7,    // Часов отдыха для статуса "Rested"
  
  // ...
};
```

-----

## ⚠️ Возможные проблемы и решения

  * **"Не найдены листы Accs или QQpoker":** Проверьте, что имена листов в таблице в точности совпадают с именами в `CONFIG`.
  * **Неверное время / 0 мин:** Проверьте формат даты и времени в логах. Скрипт понимает форматы `DD.MM.YYYY` и время `HH:MM`.
  * **Аккаунт не попадает в отчет:** Убедитесь, что на листе `Accs` заполнены поля `Nickname` и `Brain ID` (или комплект равен `S59`).

-----
